{
  "name": "usbgames-checkout",
  "version": "1.0.0",
  "private": true,
  "description": "UsbGames Stripe checkout server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test-email": "node scripts/send-test-email.js",
    "gen-secret": "node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "nodemailer": "^6.9.16",
    "resend": "^4.0.1",
    "stripe": "^17.4.0"
  }
}
